function Footer() {
  return (
    <footer className="footer">
      <p>
        DevOps Book Store — React, FastAPI, Docker,
        GitHub Actions và Azure
      </p>
    </footer>
  );
}

export default Footer;